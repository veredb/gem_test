== README

This project tests the gem pg_data_encoder, https://github.com/pbrumm/pg_data_encoder
It uses a Postgresql database and creates a binary data file that can be imported into postgres's copy from command.
